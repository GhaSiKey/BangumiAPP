<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ImageViewer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 共享元素转场 -->
        <item name="android:windowSharedElementEnterTransition">@transition/image_shared_element</item>
        <item name="android:windowSharedElementExitTransition">@transition/image_shared_element</item>
        <item name="android:windowActivityTransitions">true</item>
    </style>
</resources>
