<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bangumi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- 正常的应用主题 -->
    <style name="Theme.Bangumi" parent="Base.Theme.Bangumi" />

    <!-- 启动屏幕主题（兼容所有版本）-->
    <style name="Theme.Bangumi.Starting" parent="Theme.SplashScreen">
        <!-- 启动屏幕背景色（整个屏幕） -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- 中心显示的图标（使用专用图标，避免裁切） -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_screen_icon_u31</item>

        <!-- 退出启动屏幕后切换到的真正主题 -->
        <item name="postSplashScreenTheme">@style/Theme.Bangumi</item>
    </style>
</resources>